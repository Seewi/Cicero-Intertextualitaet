
```{r}
library(tidyverse)
```


```{r}
data <- read_csv("data/final_w_sent.csv")
allebriefe <- read_csv("data/cicero_letters.csv")
atticus <- allebriefe |> filter(corpus== "ad_atticum")
```

# Plot Addresaten
```{r}
bild_1 <- data |> 
    group_by(rec_full_name) |> 
    count() |> 
    mutate(rec_full_name = if_else(n > 1, rec_full_name, "Weitere Addressaten (Anzahl Verweise = 1)")) |> 
    group_by(rec_full_name) |> 
    arrange(desc(n)) |> 
    mutate(
        label = ifelse(n > 1, as.character(n), "")) |> 
    ungroup() |>
    mutate(
        rec_full_name = fct_reorder(rec_full_name, n),   # Reihenfolge festlegen
        label = ifelse(n > 1, as.character(n), "")
    ) |>
    ggplot(aes(x = "", y = n, fill = rec_full_name))+
    geom_col(stat = "identity", width = 1) +
    coord_polar("y") +
    geom_text(aes(label = label ),position = position_stack(vjust = 0.5))+
    theme_void()+
    guides(fill = guide_legend(reverse = TRUE)) +
    scale_fill_viridis_d(begin = 0.2, end = 0.95, option = "D") +
    labs(title = "Nennungen von Autoren pro Addressaten",  fill = "Addressaten")
```

```{r}
bild_1
```

```{r}
ggsave("images/plot_1.svg", plot = bild_1, width = 8, height = 6)

```

Wenn man unter die Lupe nimmt, wem gegenüber Cicero in seinen Briefen literarische Verweise präsentiert, dann fällt auf, dass dies primär sein Freund Atticus und sein Bruder Quintus sind. Das ist insofern nicht verwunderlich, da diese im Gesamtkorpus einen grossen Teil der Briefe ausmachen. Publius Autronius Piso und Luccius Lucceius hingegen waren römische Senatoren die zum Zeitpunkt des Erhaltens der Briefe sich der Geschichtsschreibung zugewandt hatten, was die Erwähnung Ciceros von Literatur erklären könnte. Um die genauen Gründe der intertextuellen Vergleiche zu erfassen, müsste man aber die Briefe im close-Reading erfassen. 

```{r}
data |> 
filter(
  rec_full_name %in% c("Lucius Lucceius", "Publius Autronius Paetus")
)
```

# Anzahl Briefe mit Nennungen vs. Anzahl Briefe pro Jahr

```{r}
 nbriefe<-allebriefe |>group_by(year) |> count() 

bild_2 <- data |> 
    group_by(year) |> 
    summarise(nenn_brief = n_distinct(letter_n), n = n()) |> 
    ggplot(aes(x = year)) +
    geom_bar(aes(x = year, fill = "Alle Briefe"), data = allebriefe) +
    geom_col(aes(x = year, y = nenn_brief, fill = "Briefe mit Autorenerwähnung")) +
    geom_text(aes(y = nenn_brief, label = nenn_brief), vjust = -0.5) +
    geom_text(aes(y = n, label = n), data = nbriefe, vjust = -0.7, color = "grey80") +
    scale_fill_manual(
        values = c("Alle Briefe" = "grey80", 
                   "Briefe mit Autorenerwähnung" = "#DE654E"),
        name = ""   # Legendentitel, leer lassen oder z.B. "Legende"
    ) +
    labs(title = "Briefe mit Erwähnung von Autoren vs. alle Briefe", x= "Jahr", y= "Anzahl Briefe") +
    scale_x_continuous(breaks = seq(-67, -43)) +
    
    theme_minimal()+
    theme(
        panel.grid.major.x = element_blank()
    )
```

```{r}
bild_2

```
```{r}
ggsave("images/plot_2.svg", plot = bild_2, width = 8, height = 6)

```

# Anzahl Briefe mit Nennungen vs ohne bei Atticus


```{r}
data |> 
    filter(corpus == "ad_atticum") |> 
    group_by(year) |>
    summarise(nenn_brief = n_distinct(letter_n), n = n()) |>    
    ggplot(aes(x = year)) +
    geom_bar(data = atticus, aes(x = year), fill = "grey80") +
    geom_col(aes(y = nenn_brief), fill = "red")+
    geom_text(aes(y = nenn_brief ,label = nenn_brief), vjust= -0.5)+
    #geom_point(aes(x=year, y= n))
    theme_minimal()
```

# Idee Nennungsquotient pro Jahr - noch ohne Visualisierung

```{r}

at_by_year <- atticus |> 
    group_by(year) |> 
    summarise(anzahl_brief=n())

matches_by_year <- data |> 
    filter(corpus == "ad_atticum") |> 
    group_by(year) |> 
    summarise(nenn_brief = n_distinct(letter_n))

nenn_quot <- left_join(matches_by_year, at_by_year, by = "year") |> 
    mutate(nennungsquotient = round(nenn_brief/anzahl_brief, 2))


```

# Griechisch Römisch vergleich pro Korpus 

```{r}
bild_3 <- data |> 
  count(corpus, Herkunft) |> 
  group_by(corpus) |> 
  mutate(prozent = round(n / sum(n) * 100, 1)) |> 
  ggplot(aes(y = corpus, x = n, fill = Herkunft)) +
  geom_col() +
  geom_text(aes(label = paste0(prozent, "%")),
            position = position_stack(vjust = 0.5)) +
  scale_fill_brewer(palette = "Set2") + 
  labs(title = "Sprachverteilung der genannten Autoren", x = "Anzahl Nennungen", y= "Korpus", fill = "Sprache")+
  theme_minimal()+
  theme(
        panel.grid.major.y = element_blank(),
        panel.grid.major.x = element_blank()
    )
```

```{r}
bild_3
```

```{r}
ggsave("images/plot_3.svg", plot = bild_3, width = 8, height = 6)

```
```{r}
lookuptable_herkunft <- data |> 
    select(Autor, Herkunft) |> 
    distinct(Autor, Herkunft)


```

```{r}
autoren_n <- data |> 
  filter_out(rec_full_name == "Marcus Tullius Cicero") |>  
  count(Autor) |> 
  arrange(desc(n)) |> 
  left_join(lookuptable_herkunft, by = "Autor") |> 
  rename(
    `Verteilung der Nennungen aller Autoren\n(grösseres Feld - mehr Nennungen)` = n
  )

```



```{r}
library(treemap)
treemap(autoren_n,
            index=c("Herkunft", "Autor"),
            vSize="Verteilung der Nennungen aller Autoren\n(grösseres Feld - mehr Nennungen)",
            type="index",
            bg.labels = c("transparent"),
            fontcolor.labels = c("black", "black"), 
            align.labels = list(
        c("left", "top"),
        c("center", "center")),
            palette = "Set2"
            )
```






# Idee: Jahr 54 anschauen - fast keine Nennungen bei Atticus - aber 19 Mentions 


```{r}
 bild_6 <- data |> 
    filter(year== -54) |> 
    group_by(rec_full_name) |> 
    count() |> 
    mutate(rec_full_name = if_else(n > 1, rec_full_name, "Weitere Addressaten (Anzahl Verweise = 1)")) |> 
    group_by(rec_full_name) |> 
    arrange(desc(n)) |> 
    mutate(
        label = ifelse(n > 1, as.character(n), "")) |> 
    ungroup() |>
    mutate(
        rec_full_name = fct_reorder(rec_full_name, n),   # Reihenfolge festlegen
        label = ifelse(n > 1, as.character(n), "")
    ) |>
    ggplot(aes(x = "", y = n, fill = rec_full_name))+
    geom_col(stat = "identity", width = 1) +
    coord_polar("y") +
    geom_text(aes(label = label ),position = position_stack(vjust = 0.5))+
    theme_void()+
    guides(fill = guide_legend(reverse = TRUE)) +
    scale_fill_viridis_d(begin = 0.2, end = 0.95, option = "D") +
    labs(title = "Anzahl Verweise im Jahr 54 v. Chr. pro Addressaten", fill = "Addressat")
```

```{r}

bild_6
```


```{r}
ggsave("images/plot_6.svg", plot = bild_6, width = 8, height = 6)

```
```{r}
autoren_54 <- data |>
  filter(year == -54) |>
  count(Autor) |>
  left_join(lookuptable_herkunft, by = "Autor") |>
  arrange(n, desc(Herkunft)) |>
  mutate(Autor = factor(Autor, levels = Autor)) 


```

```{r}
bild_7 <- ggplot(aes(x=n, y= Autor), data= autoren_54)+
    geom_col(aes(x=n, fill = Herkunft)) +
    scale_fill_manual(values = c("Latein" = "#FC8D62FF", 
                 "Griechisch" = "#66C2A5FF"))+
    theme_minimal()+
    labs(title = "Erwähnte Autoren im Jahr 54", x = "Anzahl Nennungen", y= "Autor", fill = "Sprache")
```

```{r}
bild_7
```
```{r}
ggsave("images/plot_7.svg", plot = bild_7, width = 8, height = 6)

```


```{r}
treemap(autoren_54,
            index= "Autor",
            vSize="n",
            type="index",
            bg.labels = c("transparent"),
            fontcolor.labels = c("black", "black"), 
        c("center", "center"),
            palette = "Greens"
            )
```


```{r}
data |> 
    distinct(Autor)
```